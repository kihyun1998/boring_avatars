{
  "issue": 110,
  "what": "the widget's buffer landing whole on the device pixel grid",
  "test": null,
  "note": [
    "The first case file for `lib/src/widget/`. Until #110 the layer had no",
    "mutation coverage at all, which is why the two regressions the",
    "completeness pass caught — a squeezed box overflowing, and a leaf that",
    "declines the hit test — had nothing standing over them.",
    "",
    "Group A is the fix itself. A1 and A2 are the two roundings; A3 is the",
    "coordinate space they are computed in.",
    "",
    "A3 is the one that earned this file, and it earned it twice. It is the",
    "half a rendered pixel cannot price: Skia with `isAntiAlias` off already",
    "rounds a destination rectangle whose width is a whole number of device",
    "pixels, so with A1 in place both A2 and A3 leave every screenshot in the",
    "suite green. They die against `snappedAvatarRect` and",
    "`debugGlobalDestination` instead — the placement asserted where it is",
    "made rather than where it is drawn. Keeping the snap despite the backend",
    "already doing it is invariant 4: leaning on Skia's rounding is the",
    "Skia-versus-Impeller dependence the package exists to refuse.",
    "",
    "Group B is what replacing a framework render object silently dropped.",
    "Neither is a wrong number; both are a default that stopped being",
    "supplied. B1 painted a 240-pixel avatar across a 20-logical box, over its",
    "neighbours; B2 stopped taps reaching an enclosing `GestureDetector`, 0",
    "against `RawImage`'s 1.",
    "",
    "`test: null` means the whole suite, and it is not narrowed here even",
    "though every assertion that kills these lives in `widget_test.dart`. The",
    "layer is new to this harness and a narrowed run would report a golden",
    "regression as a survivor — the same judgement #58 records.",
    "",
    "B3 은 레퍼런스가 준 가드다. Flutter 의 RenderEditable._snapToPhysicalPixel",
    "이 같은 식을 같은 자리에서 isFinite 로 막는다. 축퇴된 조상 변환이",
    "localToGlobal 에서 NaN 을 돌려주면 그대로 drawImageRect 의 사각형에",
    "실린다 — 파생만으로는 안 나왔고, 트래커를 읽고 나서 나왔다.",
    "",
    "**A3 was re-derived at #117, and its meaning inverted with it.** It used to",
    "mutate the coordinate space from the screen to the enclosing layer, on the",
    "belief that the screen was right. A screen capture measured the opposite —",
    "the engine rounds the layer itself, so correcting for it here applies it",
    "twice — and `snappedAvatarRect` now rounds the paint offset. There is no",
    "`global` parameter left to swap, so the case moves half a device pixel",
    "instead: the same defect the old mutation produced, expressed against the",
    "rule that replaced it.",
    "",
    "The instrument note matters as much as the case. Everything behind #110 was",
    "measured through `RenderRepaintBoundary.toImage`, which never runs the",
    "engine's composite onto the window; these mutations die against",
    "`debugPaintedDestination`, which is the render object's own record of what",
    "it drew and is not subject to that blindness.",
    "",
    "**B3 was retired at #117, and the run that retired it is the reason.** It",
    "mutated the non-finite guard in `snappedAvatarRect` — `at.isFinite ?`",
    "— and after the placement changed it SURVIVED the whole suite. Not a missing",
    "test: with the subtraction gone the branch decides nothing, since rounding",
    "carries a non-finite value through unchanged. The guard was deleted and the",
    "case with it, rather than leaving a mutation nobody can kill sitting in the",
    "file looking like coverage."
  ],
  "cases": [
    {
      "group": "A",
      "name": "A1 — the extent is not rounded onto the grid",
      "file": "lib/src/widget/boring_avatar.dart",
      "from": "    onGrid(box.width),\n    onGrid(box.height),",
      "to": "    box.width,\n    box.height,"
    },
    {
      "group": "A",
      "name": "A2 — the origin is not snapped",
      "file": "lib/src/widget/boring_avatar.dart",
      "from": "    onGrid(local.dx),\n    onGrid(local.dy),",
      "to": "    local.dx,\n    local.dy,"
    },
    {
      "group": "A",
      "name": "A3 — the snap reads the screen instead of the space it paints into",
      "file": "lib/src/widget/boring_avatar.dart",
      "from": "    onGrid(local.dx),\n    onGrid(local.dy),\n    onGrid(box.width),",
      "to": "    onGrid(local.dx) + 0.5 / devicePixelRatio,\n    onGrid(local.dy),\n    onGrid(box.width),"
    },
    {
      "group": "B",
      "name": "B1 — the drawn extent comes from the buffer, not the box",
      "file": "lib/src/widget/boring_avatar.dart",
      "from": "      box: size,",
      "to": "      box: Size(_image.width / ratio, _image.height / ratio),"
    },
    {
      "group": "B",
      "name": "B2 — the leaf declines the hit test, as RenderBox does",
      "file": "lib/src/widget/boring_avatar.dart",
      "from": "bool hitTestSelf(Offset position) => true;",
      "to": "bool hitTestSelf(Offset position) => false;"
    }
  ]
}
